import 'package:flutter/material.dart';

class RSColors {
  RSColors._();

  // Primary — Azul Marino Protección #0A1B2A
  static const primary = Color(0xFF0A1B2A);
  static const primaryLight = Color(0xFF1A3A5C); // derived lighter shade

  // Accent — Naranja Alerta #FF6A1A
  static const accent = Color(0xFFFF6A1A);

  // Semantic
  static const success = Color(0xFF2E7D32);
  static const error = Color(0xFFC62828);
  static const warning = Color(0xFFFFB300);

  // Surfaces — Blanco #FFFFFF
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF5F5F5);

  // Text — Gris Carbón #2B2B2B / Gris Medio #8E8E8E
  static const textPrimary = Color(0xFF2B2B2B);
  static const textSecondary = Color(0xFF8E8E8E);
  static const textOnPrimary = Color(0xFFFFFFFF);
  static const textOnAccent = Color(0xFFFFFFFF);

  // Border — Gris Claro #E5E5E5
  static const border = Color(0xFFE5E5E5);
  static const borderFocus = Color(0xFF0A1B2A);
}
